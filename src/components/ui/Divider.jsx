/* components/ui/Divider.jsx: application source file. See README.md for the folder responsibility. */
export default function Divider({ className = "" }) {
  return <div className={`h-px w-full bg-slate-200 ${className}`} />;
}